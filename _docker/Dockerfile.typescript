FROM node:24-slim

WORKDIR /app

# Copy package files first so Docker can cache the npm install layer.
# This means "docker compose build" only reinstalls packages when
# package.json changes, not every time you edit a script.
COPY typescript/package*.json ./
RUN npm install

COPY typescript/ .
